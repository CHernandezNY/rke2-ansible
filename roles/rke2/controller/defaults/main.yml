---
ansible_user: root
server_init_args: >-
  {% if groups['controller'] | length > 1 %}
    {% if ansible_host == hostvars[groups['controller'][0]]['ansible_host'] | default(groups['controller'][0]) %}
      --cluster-init
    {% else %}
      --server https://{{ hostvars[groups['controller'][0]]['ansible_host'] | default(groups['controller'][0]) }}:9345
    {% endif %}
    --token {{ rke2_token }}
  {% endif %}
  {{ extra_server_args | default('') }}
